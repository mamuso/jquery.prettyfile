/**
* Pretty and easy file replacement
*--------------------------------------------------------------------------*/
$.fn.prettyfile = function(options){

  var defaults = {
    placeholder: "Browse files",
		setvalue: 		true
  };
  var options = $.extend(defaults, opts||{});

	// replace me baby
  return this.each(function() {
    var $self = this;
  });
  
}